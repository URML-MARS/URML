name: language/full_engines_positive
description: >
  RFC-0260. All three engine classes plus full engine_options (whisper.cpp
  on-device STT, espeak TTS, OPUS-MT translation) validate under the default
  policy: all permissive/allied engines, no origin gate.
manifest: language_full
profiles: [home]
program:
  profile: home
  behavior:
    type: sequence
    on_error: abort_and_report
    steps:
      - move_to: { location: kitchen }
expected_validation:
  accepted: true
