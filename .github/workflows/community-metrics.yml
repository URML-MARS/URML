name: community-metrics

# Monthly community-signal collection. Runs at 09:00 UTC on the 1st of each
# month, summarizing the previous calendar month. The workflow:
#
#   1. Runs .github/scripts/collect_metrics.py to produce a full JSON dump and
#      a curated Markdown draft.
#   2. Uploads the JSON as a workflow artifact (private — visible only via the
#      Actions UI). Not committed.
#   3. Opens a draft PR with the Markdown draft so the maintainer can edit,
#      add narrative, and merge — or close the PR if the month is better not
#      recorded publicly.
#
# Manual runs are possible via `workflow_dispatch` from the Actions tab.

on:
  schedule:
    - cron: '0 9 1 * *'
  workflow_dispatch:
    inputs:
      month:
        description: 'Reporting month (YYYY-MM). Defaults to previous calendar month.'
        required: false
        default: ''

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        # actions/checkout@v4
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Set up Python
        # actions/setup-python@v5
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'

      - name: Determine period
        id: period
        env:
          MONTH_INPUT: ${{ github.event.inputs.month }}
        run: |
          if [ -n "$MONTH_INPUT" ]; then
            echo "month=$MONTH_INPUT" >> "$GITHUB_OUTPUT"
            echo "month_arg=--month $MONTH_INPUT" >> "$GITHUB_OUTPUT"
          else
            prev=$(date -u -d 'last month' +%Y-%m)
            echo "month=$prev" >> "$GITHUB_OUTPUT"
            echo "month_arg=" >> "$GITHUB_OUTPUT"
          fi

      - name: Collect metrics
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python .github/scripts/collect_metrics.py \
            --repo "${{ github.repository }}" \
            --output-dir docs/community \
            ${{ steps.period.outputs.month_arg }}

      - name: Upload full JSON as artifact
        # actions/upload-artifact@v4
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: metrics-${{ steps.period.outputs.month }}-full
          path: docs/community/metrics-${{ steps.period.outputs.month }}-full.json
          retention-days: 365

      - name: Remove full JSON before PR (artifact only, not committed)
        run: rm -f docs/community/metrics-${{ steps.period.outputs.month }}-full.json

      - name: Open draft PR with Markdown note
        # peter-evans/create-pull-request@v6
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: community/metrics-${{ steps.period.outputs.month }}
          base: main
          draft: true
          commit-message: |
            community: metrics for ${{ steps.period.outputs.month }}
          title: 'community: metrics for ${{ steps.period.outputs.month }}'
          body: |
            Auto-generated by `.github/workflows/community-metrics.yml`.

            The Markdown note is a draft. Before merging:

            - Fill in the **Narrative** section with what actually happened this period.
            - Review the auto-collected numbers for anything odd (rate-limit truncation,
              bot comments leaking into first-response calculations).
            - Decide whether this month is worth publishing. If not, close this PR.

            The full JSON dump (with all raw fields) is available as the workflow
            artifact `metrics-${{ steps.period.outputs.month }}-full` on the
            corresponding run in the Actions tab.
          add-paths: |
            docs/community/metrics-*.md
