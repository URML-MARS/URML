# Fixture: iceoryx1 IPC missing runtime_name (RFC-0385 negative).

manifest_version: "0.1"
robot_id: ground_ipc_iceoryx1_no_runtime
description: iceoryx1 IPC missing runtime_name (RFC-0385 negative).

frames:
  - name: world
    parent: null

declared_locations:
  - name: home
    pose: { x: 0.0, y: 0.0, z: 0.0 }
    frame: world

mobility:
  drive_type: differential
  max_velocity: 1.0

substrate:
  ipc:
    generation: iceoryx1
    # runtime_name intentionally omitted — RFC-0385 requires it for RouDi-based iceoryx1.

perception:
  cameras: []
  sensors: []
  object_vocabulary: []

outputs:
  named_endpoints: []
