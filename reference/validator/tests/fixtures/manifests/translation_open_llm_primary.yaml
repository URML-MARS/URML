# Fixture (RFC-0304): a permissive open LLM as the PRIMARY translation engine
# (Qwen, Apache-2.0). No gate to satisfy; a commercial deployment is fine.
# Test-only.
manifest_version: "0.1"
robot_id: translation_open_llm_primary
description: Commercial deployment translating via a permissive open LLM primary.
frames: [{ name: map, parent: null }]
declared_locations: [{ name: kitchen, pose: { x: 3.2, y: 1.0 }, frame: map }]
mobility: { drive_type: differential, max_velocity: 0.46 }
deployment: { commercial_use: true, deployment_class: production }
language:
  translation_engine_class: open_llm
  translation_engine_class_note: "Qwen3.5-Instruct, Apache-2.0"
licensing:
  components:
    - name: qwen
      license: apache_2_0
      boundary: cross_citation
      commercial_use_gate: false
