# Fixture (RFC-0260): a home robot declaring the Russian-origin Vosk STT engine.
# Accepted by the schema and without --policy; refused under the bundled
# US-federal default policy (origin gate). Test-only.
manifest_version: "0.1"
robot_id: language_vosk
description: Home robot declaring the Vosk STT engine (US-federal-gated).
frames:
  - name: map
    parent: null
declared_locations:
  - name: kitchen
    pose: { x: 3.2, y: 1.0 }
    frame: map
mobility:
  drive_type: differential
  max_velocity: 0.46
language:
  stt_engine_class: vosk
